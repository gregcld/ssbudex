import { FilterLicencePipe } from './filter-licence.pipe';

describe('FilterLicencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLicencePipe();
    expect(pipe).toBeTruthy();
  });
});
